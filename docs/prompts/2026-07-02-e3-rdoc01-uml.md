# Session: 2026-07-02 — E3 RDOC01: UML de componentes (motor de suscripciones, priority, seguros, SSE, observabilidad)

**Agente:** Claude Fable 5 (Claude Code CLI)
**Owner:** Victor (vruizz22)
**Branch:** docs/e3-rdoc01-uml (PR C de 3 — ver `plan_e3_victor.md`)
**Alcance del owner en E3:** RDOC01 — actualizar la arquitectura documentada con
lo nuevo de la E3. Cierra la serie: PR A (#63, RF03) → PR B (#64, RF02+feed) → esta.

> El agente editó el XML de `docs/arquitectura.drawio` directamente (validado
> well-formed), escribió `architecture.md §10` y marcó el hito en
> `milestones.md`. **El humano revisó los diffs y re-exporta el SVG desde la
> app de draw.io** (paso manual).

## Prompt (resumen de lo pedido al agente)

1. RDOC01 (E3): actualizar el UML de componentes con el motor de suscripciones
   (Step Functions), el broker con priority queues, el flujo de seguros
   (`package-status` + `InsuranceService`), el feed SSE y la observabilidad,
   siguiendo las convenciones del diagrama existente (AY3: `shape=module`,
   lollipop/socket con interfaces prefijo "I", supuestos como UML notes).

## Output (qué se generó / editó)

- `docs/arquitectura.drawio` (XML directo, +23 celdas, validado con
  ElementTree):
  - Subsistema **«Motor de Suscripciones (RF01 E3)»** dentro de AWS Cloud
    (contenedor ampliado hacia arriba): `AWS Step Functions (state machine)` →
    `SQS tick + DLQ` → `Lambda Dispatcher`; interfaz **ISubscriptionTick**
    (lollipop provista por NestJS Master, socket desde la Lambda); dependencia
    punteada `StartExecution (trigger RF01)` master → Step Functions; UML note
    con el contrato del trigger (idempotente, `name=subscriptionId`,
    `x-tick-secret`).
  - Componentes **EventsController (SSE)** e **InsuranceService** dentro del
    Docker Compose; interfaz **IEventFeed** (lollipop del SSE, socket desde el
    API Gateway con label `GET /events/stream (SSE)`).
  - UML notes: **RF03 priority queues** (mapeo 1/2/3 + supuesto de cumplimiento
    lado productor porque las colas `city.*` las declara la central) y
    **RF02 seguros** (package-status a origen, claims idempotentes, anti-loops)
    junto al broker; **RNF01/RNF04 observabilidad** junto a New Relic SaaS.
  - Label en la interfaz IAMQPBroker existente:
    `package-transit (priority 1–3) · package-status · ack/nack · tablas`.
- `docs/architecture.md` — nueva sección **§10 E3** : flowchart Mermaid del
  delta de componentes, tabla doble efecto de `priorityClass`
  (factor precio / prioridad AMQP), tabla del mensaje `package-status`,
  secuencia Mermaid del flujo de seguro, tabla de emisores del feed SSE,
  referencia al motor RF01 y observabilidad; tradeoffs de §10.2/§10.3.
- `docs/milestones.md` — hito «E3 — Mensajería (Victor)» con los 3 PRs y links
  a los AI-logs.
- Este AI-log.

## Decisiones (tradeoffs)

- **Editar el XML a mano** (con las convenciones ya usadas en el archivo) en
  vez de regenerar el diagrama: preserva todo el trabajo E1/E2 y el layout;
  costo: el SVG debe re-exportarse manualmente desde draw.io.
- **Supuesto RF03 como UML note** (no como cola dibujada con x-max-priority):
  la declaración de la cola no nos pertenece; documentarlo es lo honesto.
- **Motor de suscripciones como subsistema separado** dentro de AWS Cloud
  conectado por interfaces nombradas (ISubscriptionTick + StartExecution
  punteado): refleja que es infraestructura desacoplada del master (detalle
  completo delegado a `docs/step-functions.md` del owner de infra).

## Verificación

- XML del .drawio validado well-formed (ElementTree, 117 mxCells).
- Render de Mermaid/Markdown verificado en el diff de la PR.
- Pendiente manual del owner: abrir `arquitectura.drawio` en draw.io,
  ajustar posiciones finas si hace falta y re-exportar `arquitectura.svg`.
